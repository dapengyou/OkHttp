package com.test.okhttp;

import com.test.okhttp.https.HttpsUtils;
import com.test.okhttp.listener.DisposeDataHandle;
import com.test.okhttp.listener.DisposeDataListener;
import com.test.okhttp.request.CommonRequest;
import com.test.okhttp.request.RequestParams;

/**
 * 存放应用中所有post请求
 * Created by lady_zhou on 2017/9/17.
 */

public class RequestCenter {
//    private static void postRequest(String uri, RequestParams params, DisposeDataListener listener,Class<?> clazz) {
//        CommentOkHttpClient.sendRequest(CommonRequest.createGetRequest(uri, params),
//                new DisposeDataHandle(listener, clazz));
//    }
}
